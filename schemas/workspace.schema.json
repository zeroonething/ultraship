{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "UltraShip workspace",
  "description": "Owns workspace identity, umbrella vision, overall constraints, and the active product pointer. Lifecycle state, resume target, and blockers are per product; see lifecycle.schema.json.",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "name", "vision", "constraints", "active_product"],
  "properties": {
    "id": { "type": "string", "pattern": "^[a-z][a-z0-9-]*$" },
    "name": { "type": "string" },
    "vision": { "type": "string" },
    "constraints": { "type": "array", "items": { "type": "string" } },
    "active_product": { "type": ["string", "null"] }
  }
}
